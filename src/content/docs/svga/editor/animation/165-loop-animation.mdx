---
title: "Loop Animation"
description: "The three loops — preview loop, inner loop on a layer, and looping the whole animation on export — and when to use which."
slug: "docs/svga/editor/loop-animation"
sidebar:
  order: 165
draft: false
---

"Loop" means three different things in the editor. Picking the right one saves a lot of confusion:

| Loop | Where | Affects | Use it for |
| :-- | :-- | :-- | :-- |
| **Playback loop** | timeline toolbar toggle | preview in the editor only | watching your work repeat while you edit |
| **Inner loop** | one layer / a keyframe range | the document — exported | one object repeating a motion (spinner, blink, bounce) |
| **Export loop** | document **Loop** setting | the whole exported animation | the final animation repeating on the page |

## Playback Loop

Click **Loop Animation** in the timeline toolbar. When the playhead reaches the end it jumps back
to the start and playback continues. Nothing is saved to the file — it is a viewing aid.

## Inner Loop

An inner loop repeats a range of keyframes on one layer for as long as the animation lasts, while
everything else plays normally — without copying keyframes by hand.

{/* e2e-screenshot
app: svga · viewport: 1440x900 · hideDevUi: true · date: 2026-09-01
setup: setup(400, 400, {"fill": "#3d7de0", "stroke": "#1b3a63"}); click("toolsPicker.star"); move("canvas.bg", "source:200", "source:200", "canvas.bg", "drop:280", "drop:280"); addKeyframes("Star", "Rotation", {"0":0,"300":120}); click("timeline.animatedAttrs.el(nm:Star,ty:path).Rotation.loop")
crop: clip={"x":0,"y":440,"w":1085,"h":425}
annotate: annotate([{"kind":"title","theme":"red","halo":"chip","bounds":{"x":505,"y":720,"w":140,"h":24},"text":"The two keyframes","at":"bottom","offset":{"x":0,"y":15},"arrow":true,"width":2.5,"arrowFill":"accent"},{"kind":"title","theme":"red","halo":"chip","bounds":{"x":650,"y":720,"w":355,"h":24},"text":"Repeated to the end","at":"bottom","offset":{"x":0,"y":15},"arrow":true,"width":2.5,"arrowFill":"accent"},{"kind":"title","theme":"red","halo":"chip","bounds":{"x":430,"y":520,"w":455,"h":185},"text":"Inner Loop settings","at":"left","offset":{"x":-30,"y":0},"arrow":true,"width":2.5,"arrowFill":"accent"}],{"scale":1.53})
notes: scale 1.53 (1085-px crop). The inner loop is switched on by the per-row LOOP BUTTON in the timeline layer list — `timeline.animatedAttrs.el(nm:Star,ty:path).Rotation.loop` — which both enables it and opens the Inner Loop panel in one click. Keyframes are put at 0 and 300 ms deliberately: the loop repeats that range across the remaining ~700 ms, so the repeats are visible as extra diamonds; two keyframes spanning the whole duration would loop exactly once and show nothing. Crop is the BOTTOM band of the window (canvas edge + panel + timeline row) — the Inner Loop panel floats over the canvas, so a timeline-only crop would cut it. Both timeline chips use offset y 15: at 60 the second one falls outside the crop.
*/}
![The Inner Loop panel open over the canvas, with the Rotation row below showing the two original keyframes and their repeats continuing to the end of the timeline](/images/docs/editor/animation/inner-loop.png)

### Add one

- **From the layer row** — click the **loop** button at the end of the property row in the layer
  list; the Inner Loop panel opens.
- **From keyframes** — select the keyframes to repeat and choose **Add Inner Loop to Selected
  Keyframes** (**⋮** menu of the timeline toolbar).

### The Inner Loop panel

- **Before / After** — repeat the range before the first keyframe or after the last (the usual
  choice).
- **Selected Keyframes** (slider) — how many keyframe intervals form the repeated segment.
- **Alternate Direction** — every second repetition plays backwards: a back-and-forth swing
  instead of a restart.
- **To Keyframes** — bake the loop into real keyframes so you can edit individual repetitions.
- **Cancel Loop** — remove the loop; **Close and Keep** — close the panel, keep the loop.

**Remove Inner Loop from Selected Keyframes** in the **⋮** menu removes it too.

## Export Loop

Select nothing (or the `<svg/>` root) and, in the **Animation** section of the Properties panel,
switch **Loop** on. The saved animation restarts every time it finishes; off, it plays once and
holds the last frame.

It applies to every output — JSON, SVG with JS, and the CSS flavour (written as
`animation-iteration-count: infinite`) — and to the [Export](/docs/svga/editor/export) dialog's video and GIF
targets, which have their own loop options.

Related playback settings — **Direction** (normal, reverse, alternate), **Fill mode**, **Delay**,
**Iterations** — are on [Playback and triggers](/docs/svga/editor/animation#playback-and-triggers).
