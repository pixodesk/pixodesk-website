---
title: "Animation Techniques"
description: "Recipes for the less obvious animations: motion along a path, path morphing, draw-on strokes, text on a path, colour and gradients, mask reveals, filters, and instances with their own timing."
slug: "docs/svga/editor/animation-techniques"
sidebar:
  order: 170
draft: false
---

Position, scale and opacity get you far, but the memorable animations use the special ones. Each
recipe below says what to select, which property to keyframe, and what to watch out for. They all
build on [Keyframes](/docs/svga/editor/keyframes) and [Easing](/docs/svga/editor/easing-charts).

## Motion Along a Path

An object that follows a route — a car on a road, a plane on a dotted arc, a dot travelling along
a chart line.

{/* e2e-screenshot
app: svga · viewport: 1440x900 · hideDevUi: true · date: 2026-09-01
setup: setup(400, 400, {"fill": "#e0533d", "stroke": "#1b3a63"}); addBezier("exact", "M 40 300 C 110 140, 220 360, 350 120"); click("attrs.color.fill.content.type.noColor"); click("toolsPicker.star"); move("canvas.bg", "source:40", "source:300", "canvas.bg", "drop:75", "drop:335"); click("toolsPicker.select"); click("elementTree.root.el(nm:Path,ty:path).title", undefined, undefined, "meta"); click("toolbar.actionsGroup-motionPath"); click("toolbar.actionsGroup-motionPath.makeAlongPath"); input("timeline.currentFrame(unit:milliseconds).input", "450"); click("elementTree.root.el(nm:Star,ty:path).title"); color("attrs.color.fill.content.value", "rgba(224,83,61,1)"); input("attrs.transform.scale.x.input", "45"); click("attrs.color.collapse")
crop: clip={"x":0,"y":44,"w":1050,"h":830}
annotate: annotate([{"kind":"title","theme":"red","halo":"chip","bounds":{"x":520,"y":480,"w":40,"h":40},"text":"The route (a path)","at":"bottom","offset":{"x":0,"y":40},"arrow":true,"width":2.5,"arrowFill":"accent"},{"kind":"title","theme":"red","halo":"chip","bounds":{"x":638,"y":421,"w":56,"h":56},"text":"The object follows it","at":"top","offset":{"x":0,"y":-60},"arrow":true,"width":2.5,"arrowFill":"accent"},{"kind":"title","theme":"red","halo":"chip","bounds":{"x":500,"y":718,"w":425,"h":26},"text":"Motion Path keyframes","at":"bottom","offset":{"x":0,"y":15},"arrow":true,"width":2.5,"arrowFill":"accent"}],{"scale":1.48})
notes: scale 1.48 (1050-px crop). The command is `toolbar.actionsGroup-motionPath` → `.makeAlongPath` — the group must be OPENED first, its children only get ids then (same pattern as the marker picker). BOTH objects must be selected: click the mover, then meta-click the route in the tree. The route is drawn FIRST but ends up BELOW the star in the tree, which is what the page's step 1 asks for. The star loses its fill to the earlier `noColor` click (that click lands on whatever is selected), so re-select it afterwards and set the colour back with `color(…, "rgba(224,83,61,1)")`; scale 45 % keeps the traveller small enough that the dashed route stays readable. Playhead parked at 450 ms so the star sits MID-route — at 0 ms it covers the start point and the picture looks static.
*/}
![A small red star part-way along a dashed curved route on the canvas, with its Motion Path keyframes in the timeline](/images/docs/editor/animation/motion-path.png)

1. Draw the route with the Pen tool and put it **below** the moving object in the Object Tree.
2. Select both and click **Move Element Along Path** (toolbar, or the **Path** section).
3. The object's **Position** now means *distance along the path*: keyframe it from the start to
   the end, and add easing.

Tips: hide the route by giving it no stroke (it still works as a guide); reshape it later with
**Edit motion path**; **Reverse Path** flips the direction of travel. Setup details:
[Move along path](/docs/svga/editor/edit-objects#move-along-path).

## Path Morphing

Shapes can change shape over time: a logo that morphs into another, an icon that transitions
between states, a blob that breathes. The path's nodes are animated individually.

{/* e2e-screenshot-plan  — fill in, rename to e2e-screenshot, delete the div below
app: svga · viewport: 1440x900 · hideDevUi: true
show: the same path at two keyframes — a circle-ish blob and a squarish one — with the Node tool active
setup: setup(400, 400); draw a path; keyframe its shape, then at the second keyframe move nodes with the Node tool so the silhouette changes
crop: two canvas crops side by side, or one crop at each keyframe
annotate: title chips: "Keyframe 1", "Keyframe 2", "Same node count"
notes: shape morphing requires equal node counts — say so in the prose; scrub with input("timeline.currentFrame…").
*/}
<div class="shot">Screenshot: the same path at two keyframes — a circle-ish blob and a squarish one — with the Node tool active</div>

1. Select the path and press `N` (Node tool). Convert primitive shapes first
   (**Path › Object to Path**).
2. Pin the current shape (`Alt + A`) at the starting time.
3. Move the playhead, then drag nodes and handles into the new shape. A keyframe is created
   on the path shape.
4. Repeat for further states; tune the segments' easing in the Easing Function panel.

> **Keep the node count the same** between keyframes — each node is interpolated with its
> partner. Adding or removing nodes after you started animating breaks the morph. Path morphing
> is not available in the CSS-only SVG flavour (use JSON or SVG + JS).

## Trim Stroke (Draw-on)

A line that draws itself — signatures, handwriting, diagrams, loaders.

{/* e2e-screenshot
app: svga · viewport: 1440x900 · hideDevUi: true · date: 2026-09-01
setup: setup(400, 400, {"stroke": "#1b3a63"}); addBezier("exact", "M 40 250 C 70 150, 100 320, 130 220 C 155 140, 175 260, 200 230 C 230 195, 215 130, 245 150 C 275 170, 250 265, 285 250 C 320 235, 330 180, 355 160"); click("attrs.color.fill.content.type.noColor"); click("attrs.color.collapse"); input("attrs.stroke.width.input", "8"); click("attrs.stroke.expand"); click("attrs.stroke.trimStroke.setOn"); input("attrs.stroke.start/End.y.input", "50"); click("attrs.transform.collapse")
crop: clip={"x":400,"y":100,"w":1040,"h":570}
annotate: annotate([{"kind":"title","theme":"red","halo":"chip","bounds":{"x":700,"y":375,"w":24,"h":24},"text":"The line stops half-way","at":"bottom","offset":{"x":0,"y":50},"arrow":true,"width":2.5,"arrowFill":"accent"},{"kind":"title","theme":"red","halo":"chip","bounds":{"x":1057,"y":331,"w":359,"h":30},"text":"End = 50%","at":"left","offset":{"x":-70,"y":0},"arrow":true,"width":2.5,"arrowFill":"accent"}],{"scale":1.46})
notes: scale 1.46 (1040-px crop). Fill MUST be cleared via `attrs.color.fill.content.type.noColor` — a filled squiggle hides its own stroke and the trim is invisible (setup's {"fill":"none"} does not do it, tinycolor cannot parse "none"). Stroke width 8 so the half-drawn line reads at docs size. `Start/End` is ONE row with two fields: `.x.input` = start, `.y.input` = end — the 50 here is the END. The canvas chip uses hand bounds at the point where the stroke stops (no test-id for a partial stroke); re-measure if the path changes. Collapse Colors and Transform so the trim fields sit mid-panel rather than below the fold.
*/}
![A hand-written-looking curve drawn only half-way, with Trim Stroke ticked and Start/End set to 0 % and 50 % in the Properties panel](/images/docs/editor/animation/trim-stroke-draw-on.png)

1. Select the path; make sure it has a **stroke** and no fill (for handwriting, use round caps).
2. In the **Stroke** section tick **Trim Stroke Effect**.
3. Keyframe **End** from `0 %` to `100 %`. The stroke appears along the path.

Variations: keyframe **Start** from 0 % to 100 % *after* the draw-on to erase it again; animate
**Offset** with Start / End at, say, 0 / 20 % for a dash that travels around a ring (a loading
spinner); with a **Dash Array**, use **Trim All as One** to trim the dashes as one stroke.
Field reference: [Stroke › Trim stroke](/docs/svga/editor/color#stroke).

## Text Along a Path

Text that flows around a circle, along a wave, or slides along a curve.

1. Put the text on the path ([Text along path](/docs/svga/editor/text#text-along-path)).
2. Keyframe **Offset** in the **Text Path** section to move the text along the curve; for a
   full circle, 0 → 100 % makes it orbit once.
3. Animate the path's nodes as well (Node tool + auto-pin) and the text follows the moving curve.

Use **Use Glyphs** or outlines if the font must not depend on the viewer's machine.

## Color and Gradient Animation

Any colour with a clock icon animates: fill, stroke, opacity — and gradient **stops** and
gradient geometry.

- **Colour change** — switch on the clock next to the fill (Colors section), set a colour at
  each keyframe. The editor interpolates through the colour space, so red → blue passes through
  purple; add an in-between keyframe if you want a different route.
- **Gradient sweep** — animate a linear gradient's **Position** or **Rotation** for a shimmer, or a
  stop's colour for a pulsing glow. Keep the *number* of stops constant between keyframes.
- **Fade** — prefer the object's **Opacity** (Appearance section) over the colour's alpha when the
  whole object should fade; use fill / stroke opacity to fade only one of them.

## Mask Reveal

Show content by moving a mask instead of the content itself — wipes, spotlights, growing circles.

1. Make the mask ([Masks](/docs/svga/editor/masks)) — a rectangle for a wipe, a circle for a spotlight.
2. Select the mask shape and keyframe its **Position**, **Scale** or shape.

The content stays still; only the visible window moves. Combine with a trim-stroke path used as a
mask for an "ink filling in" effect.

## Filter Animation

Numeric filter settings are animatable: a **blur** that sharpens as an object arrives, a **drop
shadow** whose offset grows as the object lifts, a **saturation** that fades to grey. Switch the
clock next to the setting in the filter's entry ([Filters](/docs/svga/editor/filters)). Filters are not
available in the CSS-only SVG flavour and only partly in Lottie — check the export preview.

## Instances and Clones with Their Own Timing

A [symbol](/docs/svga/editor/symbols) instance or a [clone](/docs/svga/editor/edit-objects#clones) plays the source's
animation, but can shift it: **Start** delays it, **Stretch** speeds it up or slows it down,
**Crop Time** clips it to the instance's own range. Ten instances of one waving flag, each with a
different Start, become a stadium wave with a single animated source.

---

**Next**

- Repeat any of these forever: [Loop animation](/docs/svga/editor/loop-animation).
- Make them react to the page: [Playback and triggers](/docs/svga/editor/animation#playback-and-triggers) ·
  [Scroll-driven animation](/docs/svga/editor/animation#scroll-driven-animation).
