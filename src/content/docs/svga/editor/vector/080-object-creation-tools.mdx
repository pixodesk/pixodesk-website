---
title: "Object Creation Tools"
description: "Tools that build new objects from existing ones: Shape Builder, Repeater and Spray."
slug: "docs/svga/editor/object-creation-tools"
sidebar:
  order: 80
draft: false
---

# Object Creation Tools

Three tools make new objects out of the ones you already have: **Shape Builder** combines
overlapping shapes, **Repeater** arranges copies in lines and circles, **Spray** scatters copies
across the canvas. All three keep the result editable.

## Shape Builder

Build complex shapes from simple overlapping ones — merge, subtract or extract the regions where
they overlap, directly on the canvas, without drawing curves by hand.

{/* e2e-screenshot
app: svga · viewport: 1440x900 · hideDevUi: true · date: 2026-08-31
setup: setup(400, 400, {"fill": "#3d7fd6", "stroke": "#1b3a63"}); click("toolsPicker.ellipse"); move("canvas.bg", "source:70", "source:130", "canvas.bg", "drop:220", "drop:280"); move("canvas.bg", "source:180", "source:130", "canvas.bg", "drop:330", "drop:280"); click("toolsPicker.select"); key("a", "meta"); click("toolsPicker.shapeBuilder"); goTo("canvas.bg", "source:200", "source:205")
crop: clip={"x":250,"y":100,"w":800,"h":480}
annotate: annotate([{"kind":"box","theme":"red","bounds":{"x":655,"y":320,"w":58,"h":158},"padding":4,"color":"#08f","stroke":1.5,"fill":"none"},{"kind":"box","theme":"red","target":"toolsPicker.shapeBuilder","padding":3,"color":"#08f","stroke":1.5,"fill":"solid","fillAlpha":0.2},{"kind":"title","theme":"red","halo":"chip","bounds":{"x":655,"y":320,"w":58,"h":158},"text":"Overlap region — click to keep or remove it","at":"right","offset":{"x":0,"y":-90},"arrow":true,"width":2.5,"arrowFill":"accent"},{"kind":"title","theme":"red","halo":"chip","target":"toolsPicker.shapeBuilder","text":"Shape Builder","at":"right","offset":{"x":40,"y":0},"arrow":true,"width":2.5,"arrowFill":"accent"}])
notes: scale 1. Select the shapes BEFORE switching to Shape Builder — the tool works on the current selection and the mesh (hatched fill) only appears then. goTo() parks the pointer over the lens so the region reads as hoverable; the lens has no test-id, its bounds are hand-measured.
*/}
![Two overlapping circles in the Shape Builder tool, with the lens-shaped overlap region outlined](/images/docs/editor/tools/shape-builder.png)

1. Select two or more shapes.
2. Press `B` (Shape Builder). A mesh appears over the shapes; every region formed by the overlaps
   can be picked.
3. Click regions to add them to the new shape — or drag across several regions at once.

The toolbar has two modes and a set of quick selections:

- **Add (+)** — the clicked regions become one unified shape.
- **Remove (−)** — the clicked regions are deleted; use it to cut holes or trim overlaps.
- **All · Subtract · Intersect · Exclude · Deselect** — select all regions, prepare a subtraction,
  keep only the overlaps, keep everything except the overlaps, or clear the selection.

For the classic boolean operations on whole objects (Unite, Subtract, Intersect, Exclude) see
[Path operations](/docs/svga/editor/edit-paths#path-operations).

## Repeater

Create many synchronised copies of an object with one click — rows, grids, radial arrays — that
stay linked to the original: edit the original and every copy follows.

{/* e2e-screenshot
app: svga · viewport: 1440x900 · hideDevUi: true · date: 2026-08-31
setup: setup(400, 400, {"fill": "#3d7fd6", "stroke": "#1b3a63"}); click("toolsPicker.star"); move("canvas.bg", "source:200", "source:120", "canvas.bg", "drop:200", "drop:85"); click("attrs.repeater.repeaterEnabled.setOn"); input("attrs.repeater.copies.input", "5"); input("attrs.repeater.translation.x.input", "62"); input("attrs.repeater.scale.x.input", "88"); input("attrs.transform.translation.x.input", "95")
crop: clip={"x":400,"y":150,"w":1040,"h":520}
annotate: annotate([{"kind":"dim","target":"attrs.repeater","padding":8,"color":"#fff","fillAlpha":0.8},{"kind":"box","theme":"red","target":"attrs.repeater.copies","padding":2,"color":"#08f","stroke":1.5,"fill":"solid","fillAlpha":0.2},{"kind":"box","theme":"red","target":"attrs.repeater.translation","padding":2,"color":"#08f","stroke":1.5,"fill":"solid","fillAlpha":0.2},{"kind":"box","theme":"red","target":"attrs.repeater.scale","padding":2,"color":"#08f","stroke":1.5,"fill":"solid","fillAlpha":0.2},{"kind":"title","theme":"red","halo":"chip","target":"attrs.repeater.copies","text":"How many copies","at":"left","arrow":true,"width":2.5,"arrowFill":"accent"},{"kind":"title","theme":"red","halo":"chip","target":"attrs.repeater.translation","text":"Offset per copy","at":"left","arrow":true,"width":2.5,"arrowFill":"accent"},{"kind":"title","theme":"red","halo":"chip","target":"attrs.repeater.scale","text":"Scale per copy","at":"left","arrow":true,"width":2.5,"arrowFill":"accent"}])
notes: scale 1.45 (1040-px crop). CHANGED FROM THE PLAN: a rotating ring needs the repeater ORIGIN placed at the ring centre in the shape's own coordinates, which is fiddly and kept throwing copies off-canvas; a translation + scale falloff shows the same idea reliably. Enabling the repeater switches the active tool to repeaterTool. Rows: attrs.repeater.copies / .origin / .translation / .rotation / .repeaterSkew / .scale.
*/}
![A star repeated five times by the Repeater, each copy offset and slightly smaller, with the Repeater section annotated](/images/docs/editor/tools/repeater.png)

1. Select the object (several objects also work — they repeat as one unit).
2. Group it: **Object › Group** (`⌘ / Ctrl + G`).
3. Tick **Repeater** in the Properties panel — or press `Y` for the Repeater tool.

Then set, in the **Repeater** section:

| Property | Meaning |
| :-- | :-- |
| **Copies** | how many instances to generate |
| **Origin** | the pivot the copies are arranged around |
| **Translation** | the distance and direction between consecutive copies |
| **Rotation** | the angle added with each copy |
| **Scale** | the size change from one copy to the next |

The toolbar (Repeater tool) has presets and shortcuts: **Circle** (a perfect radial array),
**Line** (a straight row), **Add one clone / Remove one clone**, **Cancel Repeat** (removes the
effect from the group). The group's shape and the repeater settings are edited independently, so
you can restyle the original at any time.

## Spray

Scatter copies of an object with organic, randomised size, rotation and opacity — leaves, stars,
dust, confetti.

{/* e2e-screenshot
app: svga · viewport: 1440x900 · hideDevUi: true · date: 2026-08-31
setup: setup(400, 400, {"fill": "#f0be3c", "stroke": "#1b3a63"}); click("toolsPicker.star"); move("canvas.bg", "source:60", "source:340", "canvas.bg", "drop:60", "drop:315"); click("toolsPicker.sprayTool"); move("canvas.bg", "source:120", "source:130", "canvas.bg", "drop:300", "drop:250")
crop: clip={"x":240,"y":40,"w":800,"h":560}
annotate: annotate([{"kind":"box","theme":"red","bounds":{"x":271,"y":55,"w":604,"h":26},"padding":3,"color":"#08f","stroke":1.5,"fill":"solid","fillAlpha":0.2},{"kind":"box","theme":"red","bounds":{"x":762,"y":400,"w":110,"h":110},"padding":2,"color":"#08f","stroke":1.5,"fill":"none"},{"kind":"title","theme":"red","halo":"chip","bounds":{"x":271,"y":55,"w":604,"h":26},"text":"Brush size, amount, rotation, scale and opacity range","at":"bottom","offset":{"x":0,"y":10},"arrow":true,"width":2.5,"arrowFill":"accent"},{"kind":"title","theme":"red","halo":"chip","bounds":{"x":762,"y":400,"w":110,"h":110},"text":"Brush — drag to scatter copies","at":"left","offset":{"x":0,"y":-90},"arrow":true,"width":2.5,"arrowFill":"accent"}])
notes: scale 1. The Spray tool sprays COPIES OF THE SELECTED OBJECT — with nothing selected the canvas just shows "Select objects to be cloned when sprayed" and the drag does nothing. Draw the source shape first (it stays selected), then switch to sprayTool and drag. Toolbar: toolbar.size / .amount / .rotationTurn / .minScale / .maxScale / .minOpacity / .maxOpacity.
*/}
![Stars scattered by the Spray tool at varying size and opacity, with the brush circle and the spray options in the toolbar](/images/docs/editor/tools/spray.png)

1. Select the object and group it (`⌘ / Ctrl + G`). A group can hold several shapes — the tool
   sprays the cluster as one unit.
2. Pick the **Spray** tool in the Tools panel.
3. Click, or drag across the canvas, to distribute copies.

The copies are SVG `<use>` clones of the original: hundreds of them stay light, and changing the
original inside the group updates every sprayed copy. Toolbar settings:

| Setting | Meaning |
| :-- | :-- |
| **Spray Radius** | the size of the area each click covers |
| **Amount** | how many copies per click / drag step |
| **Rotation** | random rotation applied to each copy |
| **Min / Max Scale** | the size range the copies are picked from |
| **Min / Max Opacity** | the transparency range — fade-outs, depth, atmosphere |

---

**Next**

- Turn a repeated cluster into a reusable [Symbol](/docs/svga/editor/symbols) to place it elsewhere.
- Linked copies of a single object without a group: [Clones](/docs/svga/editor/edit-objects#clones).
