---
title: "Filters"
description: "Filter effects — shadows, blur, colour adjustments and more: add a preset, tune it, build your own from primitives, share it, remove it."
slug: "docs/svga/editor/filters"
sidebar:
  order: 120
draft: false
---

Filters add rendered effects to any object or group — drop shadows, blur, glow, colour
adjustments, distortion — without changing the object's geometry. They are SVG filters: a chain
of **primitives** stored once and shared by every object that uses the same filter.

{/* e2e-screenshot
app: svga · viewport: 1440x900 · hideDevUi: true · date: 2026-08-31
setup: setup(400, 400, {"fill": "#f0be3c", "stroke": "#1b3a63"}); click("toolsPicker.star"); move("canvas.bg", "source:200", "source:200", "canvas.bg", "drop:200", "drop:110"); click("attrs.svgFilter.addFilter")
crop: clip={"x":380,"y":52,"w":1060,"h":620}
annotate: annotate([{"kind":"box","theme":"red","bounds":{"x":722,"y":150,"w":488,"h":220},"padding":6,"color":"#08f","stroke":1.5,"fill":"solid","fillAlpha":0.2},{"kind":"box","theme":"red","target":"attrs.svgFilter.addFilter","padding":3,"color":"#08f","stroke":1.5,"fill":"solid","fillAlpha":0.2},{"kind":"title","theme":"red","halo":"chip","bounds":{"x":722,"y":150,"w":488,"h":220},"text":"Filter presets — blur, drop shadow, glow…","at":"left","offset":{"x":0,"y":-70},"arrow":true,"width":2.5,"arrowFill":"accent"},{"kind":"title","theme":"red","halo":"chip","target":"attrs.svgFilter.addFilter","text":"Filters section — + adds one","at":"left","arrow":true,"width":2.5,"arrowFill":"accent"}])
notes: scale 1.45 (1060-px crop). CHANGED FROM THE PLAN: showing an applied drop shadow + blur needs a preset to be chosen, and the preset thumbnails have NO test-ids — a synthetic click at their coordinates only dismissed the popup. This shows the step the reader actually has to find: the Filters + button and the preset gallery it opens. Annotate immediately after the click that opens the popup — any other click closes it.
*/}
![The Filters section with the + button pressed, showing the gallery of filter presets](/images/docs/editor/filters/filter-presets.png)

## Add a Filter

1. Select the object or group.
2. In the Properties panel, open the **Filter** section and click **Add Filter**.
3. Pick a preset. The effect is applied immediately.

Presets: **Drop Shadow · Inner Shadow · Outline · Gaussian Blur · Motion Blur · Pixelate · Warp
· Morphology · Saturate · Hue Rotate · Invert · Brightness · Contrast · Opacity · Posterize**.
Filters already in the document are listed at the top of the same dropdown — choose one to
share it with another object.

Several filters can stack on one object; each is its own entry in the Filter section.

## Tune a Filter

Expand the filter's entry in the Filter section:

- **Filter Region** — **Position** (X, Y) and **Size** (X, Y), in % or px. Shadows and blurs
  extend beyond the object; enlarge the region if the effect looks cut off.
- **Primitive Units** — the coordinate system the primitives use (usually px).
- Preset-specific settings — for shadows: **Shadow Radius** (softness), **Offset** (X, Y) and
  **Shadow Color**; for blurs: the blur amount; for colour presets: the amount.

Numeric filter settings are animatable — a blur that sharpens, a shadow that grows.

## Build Your Own Filter

Click **Edit** on a filter entry to open the filter editor: a graph of the primitives in order.
Select a primitive to change its parameters, drag to reorder, and click **+** to add one:

| Group | Primitives |
| :-- | :-- |
| **Sources** | Flood, Turbulence, Image |
| **Modifiers** | Blur, Drop Shadow, Color Matrix, Component Transfer, Morphology, Convolve Matrix, Offset, Tile |
| **Combiners** | Blend, Composite, Merge, Displacement Map |
| **Lighting** | Specular Lighting, Diffuse Lighting, Point Light, Spot Light, Distant Light |

{/* e2e-screenshot-plan  — BLOCKED (re-checked 2026-09-01), needs an app change first
app: svga · viewport: 1440x900 · hideDevUi: true
show: the filter editor — three primitives in a chain, one selected with its parameters
setup: setup(400, 400); draw a shape; click("attrs.svgFilter.addFilter") opens the PRESET gallery — and stops there
crop: clip covering the editor
annotate: title chips: "Primitive chain", "Selected primitive", "Its parameters"
notes: BLOCKER — a filter cannot be applied by script, so the editor can never be reached. `attrs.svgFilter.addFilter`
  opens the preset gallery, but its 15 thumbnails have NO test-ids even while the popup is OPEN (verified twice with
  list_ids — 2026-08-31 and again 2026-09-01; contrast the marker picker, which DOES expose `…markerPicker.libMarker0-8`
  once open, and is why markers could be shot). Until a preset thumbnail carries a `data-test-id` this shot is
  impossible — ask the owner before adding one. The preset gallery itself is already documented by
  filter-presets.png above; do not re-shoot that as a substitute for the editor.
*/}
<div class="shot small">Screenshot: the filter editor — three primitives in a chain, one selected with its parameters</div>

## Shared Filters and the Filters Section

The **Filters** section of the Objects panel lists every filter in the document: its name, how
many objects use it, and an expandable tree of its primitives (`<feGaussianBlur/>`,
`<feOffset/>`, …). Editing a shared filter changes all objects that reference it; if you want to
change just one object, add a fresh preset to it instead.

## Remove a Filter

In the object's **Filter** section, click the **bin** icon on the entry. The object loses the
effect; the filter definition is deleted too when no other object uses it, otherwise it stays in
the Filters section.

## Filters and the Saved File

Filters are native SVG and are kept in SVG and JSON output. Lottie supports only a subset (drop
shadow, blur); other primitives are dropped on export — the export preview warns you. See
[SVG ↔ Lottie support](/docs/svga#svg--lottie-support).
