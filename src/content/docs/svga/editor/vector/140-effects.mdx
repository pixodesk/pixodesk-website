---
title: "Effects"
description: "Every effect an object can carry — shape presets, repeater, clone, mask effect, clip path, trim stroke, motion path, text on path, glyph text, gradients, filters — in one catalogue with where to find and how to animate each."
slug: "docs/svga/editor/effects"
sidebar:
  order: 140
draft: false
---

# Effects

An **effect** is something attached to an object that changes how it is drawn without changing
what it is: a rectangle stays a rectangle while a repeater makes twelve of it, a path stays a path
while a trim stroke draws it on. Effects live in the Properties panel next to the object's own
properties, most of them animate, and they are what the Pixodesk player and the file format
understand as effects. This page is the catalogue; each row links to the full how-to.

{/* e2e-screenshot
app: svga · viewport: 1440x900 · hideDevUi: true · date: 2026-09-01
setup: setup(400, 400, {"fill": "#3d7de0", "stroke": "#1b3a63"}); click("toolsPicker.star"); move("canvas.bg", "source:130", "source:130", "canvas.bg", "drop:200", "drop:200"); click("toolsPicker.select"); click("attrs.repeater.repeaterEnabled.setOn"); click("attrs.stroke.expand"); click("attrs.stroke.trimStroke.setOn"); click("attrs.color.collapse"); click("attrs.transform.collapse"); input("attrs.stroke.width.input", "6"); input("attrs.stroke.start/End.y.input", "60")
crop: clip={"x":400,"y":100,"w":1040,"h":570}
annotate: annotate([{"kind":"title","theme":"red","halo":"chip","bounds":{"x":1057,"y":229,"w":359,"h":110},"text":"Trim Stroke","at":"left","offset":{"x":-70,"y":0},"arrow":true,"width":2.5,"arrowFill":"accent"},{"kind":"title","theme":"red","halo":"chip","bounds":{"x":1057,"y":460,"w":359,"h":110},"text":"Repeater","at":"left","offset":{"x":-70,"y":0},"arrow":true,"width":2.5,"arrowFill":"accent"}],{"scale":1.46})
notes: scale 1.46 (1040-px crop). TWO effects, not three: the Filters gallery thumbnails have NO test-ids even while the popup is open (checked again 2026-09-01 with list_ids — unlike the marker picker, which does expose libMarker0-8), so a filter preset cannot be applied by script; the Filters row also falls below the fold once Stroke and Repeater are expanded. Do not retry without ids. Trim Stroke must be made VISIBLE to be worth showing — stroke width 6 and Start/End end at 60 %, so each outline stops short; at width 1 and 0-100 % the tick in the panel is the only evidence. Repeater is switched on BEFORE collapsing Colors/Transform, and it also switches the active tool to the Repeater (hence the extra canvas handles). Trim Stroke and Dash Array are mutually exclusive (SvgStrokeAttrs.tsx) — do not try to show both.
*/}
![A star with Trim Stroke and the Repeater applied: three copies on the canvas, each outline drawn only part-way round, with both effect sections open in the Properties panel](/images/docs/editor/vector/effects-stacked.png)

| Effect | What it does | Where you set it | Animates | In Lottie |
| :-- | :-- | :-- | :-: | :-: |
| [Shape preset](#shape-preset) | keeps a star / polygon / spiral parametric | Shape section | ✓ | ✓ |
| [Repeater](#repeater) | many synchronised copies in a line or circle | Repeater section · Repeater tool | ✓ | ✓ |
| [Clone](#clone) | a linked copy of another object | Edit › Clone | ✓ | ✓ |
| [Mask effect](#mask-effect) | one object controls another's visibility, both stay editable | Object › Add Mask Effect | ✓ | ✓ |
| [Clip path](#clip-path) | a hard-edged cut-out by another object's outline | Object › Make Clip Path | ✓ | ✓ |
| [Trim stroke](#trim-stroke) | shows only part of the stroke — draw-on / erase | Stroke section | ✓ | ✓ |
| [Motion path](#motion-path) | the object travels along a path | Move Element Along Path | ✓ | ✓ |
| [Text on path](#text-on-path) | text follows a curve | Text Path section | ✓ | partly |
| [Glyph text](#glyph-text) | letters embedded as outlines | Font section › Use Glyphs | ✓ | ✓ |
| [Gradients](#gradients) | fills and strokes that blend colours | Colors section | ✓ | ✓ |
| [Filters](#filters) | shadows, blur, colour adjustments | Filter section | ✓ | partly |
| [Instance timing](#instance-timing) | a symbol instance or clone plays its source later, faster, cropped | Animation section of the instance | — | ✓ |

*In Lottie* is the export side; the exact matrix is in
[SVG ↔ Lottie support](/docs/svga#svg--lottie-support). All effects are kept in JSON and
SVG + JS output; the CSS-only SVG flavour keeps the static result of every effect and animates
a subset — the preview warns you.

## Shape Preset

A star, polygon or spiral drawn with the Shape tool keeps its recipe — **Points Count** and its
radii — so you can change the number of points later instead of editing nodes, and keyframe them
(a five-point star growing an extra point, a spiral winding up). Converting the shape to a path
(**Path › Object to Path**) bakes the preset. → [Shape presets](/docs/svga/editor/draw-on-canvas#shape-presets-stars-polygons-spirals)

## Repeater

Turns a group into **N** copies laid out by **Translation**, **Rotation** and **Scale** steps around
an **Origin**. Edit the group's content and every copy follows; animate the count or the steps for
unfolding fans, ticking clocks, growing grids. → [Repeater](/docs/svga/editor/object-creation-tools#repeater)

## Clone

A `<use>` reference to another object — the object drawn again elsewhere, at almost no cost, and
always identical to its source. Clones have their own transform and can lock to or ignore the
source's position. → [Clones](/docs/svga/editor/edit-objects#clones)

## Mask Effect

The lower of two objects defines where the upper one is visible. Unlike a classic mask, the
masking object stays a normal, visible, animatable object, and the link is a reference you can
remove without changing either. → [Mask effect](/docs/svga/editor/masks#mask-effect)

## Clip Path

The top object's outline becomes a hard-edged window for the object below; the clipping shape
leaves the canvas. Animated single-path clips keep their animation. → [Clip path](/docs/svga/editor/masks#clip-path)

## Trim Stroke

**Start**, **End** and **Offset** (in % of the path length) decide which part of a stroke is
drawn. Keyframe End 0 → 100 % and the line draws itself; animate Offset for a travelling dash;
**Trim All as One** treats a dashed stroke as one line. → [Trim stroke](/docs/svga/editor/color#stroke) ·
[Draw-on technique](/docs/svga/editor/animation-techniques#trim-stroke-draw-on)

## Motion Path

Attaches an object to a path so its **Position** becomes a distance along that path. Keyframe
the position and the object travels; the route stays editable. → [Move along path](/docs/svga/editor/edit-objects#move-along-path) ·
[Technique](/docs/svga/editor/animation-techniques#motion-along-a-path)

## Text on Path

Lays a single line of text along a path, with **Offset** to slide it, **Overflow** clip / extend,
and the browser's text-path layout controls. → [Text along path](/docs/svga/editor/text#text-along-path)

## Glyph Text

Embeds the letters actually used as vector outlines, so the file needs no font on the viewer's
machine and text can be converted to shapes. The outlines are listed under **Used Glyphs** in the
Objects panel. → [Text › Use Glyphs](/docs/svga/editor/text#text-objects)

## Gradients

Linear and radial gradients for fills and strokes, with animatable stops (colour, position,
alpha) and geometry (start / end, centre, radius, focal point). → [Linear and radial gradients](/docs/svga/editor/color#linear-and-radial-gradients)

## Filters

SVG filter chains — drop shadow, blur, glow, colour adjustments, distortions — shared between
objects, with animatable numeric settings. → [Filters](/docs/svga/editor/filters)

## Instance Timing

A symbol instance or clone can **Start** its source's animation later, **Stretch** its speed, and
**Crop Time** to its own range — one animated source, many differently timed copies.
→ [Symbols › Instance properties](/docs/svga/editor/symbols#place-instances) · [Technique](/docs/svga/editor/animation-techniques#instances-and-clones-with-their-own-timing)
