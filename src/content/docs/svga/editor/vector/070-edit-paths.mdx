---
title: "Edit Paths"
description: "The Node tool, node commands, boolean path operations, aligning nodes, and the commands that combine, break apart, reverse and mirror paths."
slug: "docs/svga/editor/edit-paths"
sidebar:
  order: 70
draft: false
---

Any shape can become a path, and any path can be reshaped node by node. This section covers the
**Node** tool, the commands in the **Path** menu, and the boolean operations that build one shape
out of several.

> **Rectangles, ellipses, stars and text are not paths yet.** To edit their nodes, convert them
> first: **Path › Object to Path** (also *Convert to Curves* / *Text to Bezier Shape* in the
> toolbar). The shape keeps its look and becomes fully editable.

## Node Tool

Press `N`, click a path, and its nodes and handles appear.

{/* e2e-screenshot
app: svga · viewport: 1440x900 · hideDevUi: true · date: 2026-08-31
setup: setup(400, 400, {"fill": "#3d7fd6", "stroke": "#1b3a63"}); addBezier("exact", "M80,280C80,140 200,80 260,140C320,200 300,300 200,320z"); click("toolsPicker.editBezier"); click("canvas.el.path(0).bezierControlPoint(1)")
crop: clip={"x":250,"y":40,"w":820,"h":560}
annotate: annotate([{"kind":"box","theme":"red","target":"toolbar.editBzPath","padding":3,"color":"#08f","stroke":1.5,"fill":"solid","fillAlpha":0.2},{"kind":"box","theme":"red","target":"canvas.el(nm:Path,ty:path)","padding":12,"color":"#08f","stroke":1.5,"fill":"none"},{"kind":"title","theme":"red","halo":"chip","target":"toolbar.editBzPath","text":"Node commands: add, remove, corner, smooth","at":"bottom","offset":{"x":60,"y":0},"arrow":true,"width":2.5,"arrowFill":"accent"},{"kind":"title","theme":"red","halo":"chip","target":"canvas.el(nm:Path,ty:path)","text":"Nodes and their handles","at":"right","offset":{"x":0,"y":-60},"arrow":true,"width":2.5,"arrowFill":"accent"}])
notes: scale 1. The Node tool is toolsPicker.editBezier; its command bar is toolbar.editBzPath (bzAddNode, bzRemoveNode, bzBreakAtPoints, bzPathStart, bzReversePath, bzMakeLineCorner / CurveCorner / Smooth / Symmetric / Asymmetric). Selecting a node shows its handles. addBezier("exact", d) gives a repeatable path.
*/}
![A path in the Node tool with a node selected and its direction handles showing, and the node command bar in the toolbar](/images/docs/editor/paths/node-tool.png)

- **Select** — click a node; **Shift + click** adds or removes nodes; **drag** on empty canvas
  draws a selection box; `Ctrl + A` selects all nodes of the path.
- **Move** — drag selected nodes; hold **Shift** to constrain to 90°.
- **Reshape a segment** — drag the segment itself; the neighbouring handles follow.
- **Handles** — drag a handle to change the curve on that side of the node; **double-click** a
  handle to remove it and make the node a sharp corner.
- **Add a node** — double-click the path where you want it.

The Node tool works in animation too: pin a state, move nodes, and the path **morphs** between
keyframes — see [Path animation](/docs/svga/editor/animation-techniques#path-morphing).

## Node Commands

With nodes selected, these live in the **Path** menu and in the toolbar:

| Command | What it does |
| :-- | :-- |
| **Add Node** | inserts a node on the selected segment |
| **Remove Node** | deletes the node, keeping the path shape as close as possible |
| **Break at Selected Points** | splits the path at the selected nodes into separate segments |
| **Make Line Corner** | corner node with straight segments, no handles |
| **Make Curve Corner** | corner node with independent handles on each side |
| **Make Smooth** | handles aligned, so the curve flows through the node |
| **Make Symmetric** | handles aligned and equal in length — a balanced curve |
| **Make Asymmetric** | handles aligned but with independent lengths |
| **Path Start** | makes the selected node the first node of the path |
| **Reverse Path** | flips the node order (matters for markers, trim and motion along the path) |

## Path Operations

Boolean operations combine whole objects into one. Select **two or more** objects and choose the
command from the **Path** menu; the shapes are processed relative to the **backmost** object.

{/* e2e-screenshot
app: svga · viewport: 1440x900 · hideDevUi: true · date: 2026-08-31
setup: setup(400, 400, {"fill": "#3d7fd6", "stroke": "#1b3a63"}); click("toolsPicker.ellipse"); move("canvas.bg", "source:60", "source:150", "canvas.bg", "drop:180", "drop:270"); move("canvas.bg", "source:140", "source:150", "canvas.bg", "drop:260", "drop:270"); click("toolsPicker.select"); key("a", "meta"); click("toolbar.actionsGroup-shapePath")
crop: clip={"x":300,"y":40,"w":760,"h":480}
annotate: annotate([{"kind":"dim","bounds":{"x":710,"y":120,"w":214,"h":264},"padding":8,"color":"#fff","fillAlpha":0.78},{"kind":"box","theme":"red","target":"toolbar.actionsGroup-shapePath","padding":2,"color":"#08f","stroke":1.5,"fill":"solid","fillAlpha":0.2},{"kind":"box","theme":"red","bounds":{"x":721,"y":135,"w":192,"h":122},"padding":2,"color":"#08f","stroke":1.5,"fill":"solid","fillAlpha":0.2},{"kind":"box","theme":"red","bounds":{"x":721,"y":280,"w":192,"h":92},"padding":2,"color":"#08f","stroke":1.5,"fill":"solid","fillAlpha":0.2},{"kind":"title","theme":"red","halo":"chip","target":"toolbar.actionsGroup-shapePath","text":"Path operations","at":"left","offset":{"x":-130,"y":0},"arrow":true,"width":2.5,"arrowFill":"accent"},{"kind":"title","theme":"red","halo":"chip","bounds":{"x":721,"y":135,"w":192,"h":122},"text":"Unite · Subtract · Intersect · Exclude","at":"left","arrow":true,"width":2.5,"arrowFill":"accent"},{"kind":"title","theme":"red","halo":"chip","bounds":{"x":721,"y":280,"w":192,"h":92},"text":"Combine · Break Apart · Stroke to Path","at":"left","arrow":true,"width":2.5,"arrowFill":"accent"}])
notes: scale 1. CHANGED FROM THE PLAN: showing the four *results* side by side would mean building four separate shape pairs and applying one op to each — a lot of setup for little gain. This shows the two overlapping circles selected with the Shape Path menu open, which is what the reader has to find. Menu ids: toolbar.actionsGroup-shapePath.unite / .subtract / .intersect / .exclude / .combine / .breakApart / .strokeToPath. Select at least two shapes or the menu is disabled.
*/}
![Two overlapping circles selected with the Shape Path menu open, showing Unite, Subtract, Intersect, Exclude and the compound commands](/images/docs/editor/paths/boolean-ops.png)

| Operation | Shortcut | Result |
| :-- | :-- | :-- |
| **Unite** | `Alt + U` | one shape covering everything |
| **Subtract** | `Alt + S` | the back object minus the front objects |
| **Intersect** | `Alt + I` | only where all objects overlap |
| **Exclude** | `Alt + E` | everything except the overlaps (transparent overlaps) |
| **Stroke to Path** | `Alt + P` | the stroke becomes a filled shape of its own |
| **Object to Path** | — | a primitive shape or text becomes an editable bezier path |

For an interactive way to do the same on the canvas, use the
[Shape Builder](/docs/svga/editor/object-creation-tools#shape-builder).

## Combine and Break Apart

**Combine** (`Alt + K`) joins selected paths into one *compound* path: the shapes stay separate
sub-paths (nothing is merged), but they act as a single object — and where sub-paths overlap, the
overlap is cut out (even-odd fill). Use it for letters with holes, rings, frames.

**Break Apart** (`Shift + Alt + K`) does the opposite: a compound path becomes independent paths
again.

## Mirror Path

Draw half a symmetrical shape and let the editor complete it:

1. Draw one half with the Pen tool, as an open path.
2. With the Node tool, select the two open end nodes.
3. Choose **Path › Mirror Path**.

The path is mirrored across the line between the two nodes and closed — ideal for icons, logos
and anything symmetrical.

{/* e2e-screenshot
app: svga · viewport: 1440x900 · hideDevUi: true · date: 2026-08-31
setup: setup(400, 400, {"fill": "#3d7fd6", "stroke": "#1b3a63"}); addBezier("exact", "M200,310C120,230 110,150 160,130C185,120 200,150 200,170"); click("toolsPicker.editBezier"); click("canvas.el.path(0).bezierControlPoint(0)"); click("canvas.el.path(0).bezierControlPoint(2)", undefined, undefined, "shift"); click("toolbar.makeSymmetricShapeAround2Points")
crop: clip={"x":330,"y":40,"w":760,"h":560}
annotate: annotate([{"kind":"box","theme":"red","target":"toolbar.makeSymmetricShapeAround2Points","padding":3,"color":"#08f","stroke":1.5,"fill":"solid","fillAlpha":0.2},{"kind":"box","theme":"red","target":"canvas.el(nm:Path,ty:path)","padding":10,"color":"#08f","stroke":1.5,"fill":"none"},{"kind":"title","theme":"red","halo":"chip","target":"toolbar.makeSymmetricShapeAround2Points","text":"Make Symmetric Shape — mirrors the half you drew","at":"bottom","offset":{"x":40,"y":0},"arrow":true,"width":2.5,"arrowFill":"accent"},{"kind":"title","theme":"red","halo":"chip","target":"canvas.el(nm:Path,ty:path)","text":"Half a heart, mirrored and closed","at":"right","offset":{"x":0,"y":-40},"arrow":true,"width":2.5,"arrowFill":"accent"}])
notes: scale 1. The command is toolbar.makeSymmetricShapeAround2Points on the Node tool. It needs EXACTLY TWO nodes selected (the ends of the open half) — Cmd+A selects them all and the command then does nothing. Click the first node, shift-click the second, then press it.
*/}
![Half a heart drawn as an open path, mirrored and closed into a whole heart by Make Symmetric Shape](/images/docs/editor/paths/mirror-close.png)

## Align and Distribute Nodes

With several nodes selected, the **Path** menu aligns them — **Left · Center · Right · Top ·
Middle · Bottom** (to the outermost selected node or the centre) — and distributes them evenly
**Horizontally** or **Vertically**. The same buttons are in the toolbar of the Node tool.

---

**Next**

- Turn strokes into draw-on animations: [Trim stroke](/docs/svga/editor/color#stroke).
- Morph one shape into another: [Path animation](/docs/svga/editor/animation-techniques#path-morphing).
