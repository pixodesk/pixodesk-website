---
title: "Symbols"
description: "Reusable symbols: one definition, many linked instances — create, place, edit, detach and delete them."
slug: "docs/svga/editor/symbols"
sidebar:
  order: 90
draft: false
---

# Symbols

A **symbol** is a piece of artwork defined once and placed many times. Every placed copy — an
**instance** — points at the same definition, so editing the symbol updates all of them, and the
file stays small no matter how many you place. In SVG terms: a `<symbol>` in `<defs>` and `<use>`
instances.

{/* e2e-screenshot
app: svga · viewport: 1440x900 · hideDevUi: true · date: 2026-08-31
setup: setup(400, 400, {"fill": "#f0be3c", "stroke": "#1b3a63"}); click("toolsPicker.star"); move("canvas.bg", "source:100", "source:150", "canvas.bg", "drop:100", "drop:105"); click("toolsPicker.select"); click("toolbar.actionsGroup-addSymbol.makePrecomp"); key("d", "meta"); move("canvas.el(nm:Clone_2,ty:use)", 120, 0); key("d", "meta"); move("canvas.el(nm:Clone_3,ty:use)", 170, 0); click("elementTree.blocks.header.title")
crop: clip={"x":0,"y":40,"w":1000,"h":620}
annotate: annotate([{"kind":"box","theme":"red","bounds":{"x":7,"y":113,"w":244,"h":96},"padding":2,"color":"#08f","stroke":1.5,"fill":"solid","fillAlpha":0.2},{"kind":"box","theme":"red","target":"elementTree.blocks","padding":3,"color":"#08f","stroke":1.5,"fill":"solid","fillAlpha":0.2},{"kind":"title","theme":"red","halo":"chip","bounds":{"x":7,"y":113,"w":244,"h":96},"text":"Three instances of one symbol","at":"right","offset":{"x":180,"y":-40},"arrow":true,"width":2.5,"arrowFill":"accent"},{"kind":"title","theme":"red","halo":"chip","target":"elementTree.blocks","text":"Symbols section — the master copy","at":"right","offset":{"x":180,"y":0},"arrow":true,"width":2.5,"arrowFill":"accent"}])
notes: scale 1.4 (1000-px crop). Make Symbol is toolbar.actionsGroup-addSymbol.makePrecomp (NOT in a menu with a "symbol" id — the group also holds svgClone, svgMakeMarker, svgMakePattern). Instances are ty:use and are named Clone / Clone_2 / …; the Symbols section of the Objects panel is elementTree.blocks and starts collapsed.
*/}
![Three instances of one star symbol on the canvas, with the Symbols section of the Objects panel showing the master copy](/images/docs/editor/symbols/symbol-instances.png)

Use symbols for anything repeated — icons, bullets, characters, UI elements — and for animated
building blocks: an instance can start its symbol's animation later, play it faster, or crop it.

## Create a Symbol

1. Select one or more objects.
2. Choose **Object › Make Symbol** (`⌘ / Ctrl + P`), or click **Make Symbol** in the toolbar
   (Selection or Transform tool).

The selection is replaced by an instance (a `<use>` layer in the Object Tree) and the definition
appears in the **Symbols** section of the Objects panel with a thumbnail.

## Place Instances

- **Drag** the symbol's thumbnail from the Symbols section onto the canvas.
- **Duplicate** an existing instance: **Edit › Duplicate** (`⌘ / Ctrl + D`) or copy / paste.

Every instance is linked. Each one has its own position, rotation, scale, opacity and — if the
symbol's shapes use *unset* colours — its own fill and stroke.

### Instance properties

With an instance selected, the Properties panel adds:

| Section | Setting | Meaning |
| :-- | :-- | :-- |
| **Symbol** | name, **Edit Symbol** | which symbol it shows; enter Symbol View |
| **Display** | **Offset** (X, Y) | shifts the content inside the instance box |
| | **Custom Size** | override the symbol's natural width / height |
| **Animation** | **Start** (ms) | delay before the symbol's own animation starts |
| | **Stretch** (%) | playback speed of the symbol's animation (100 % = normal) |
| | **Crop Time** | clip the symbol's animation to the instance's time range |

## Edit a Symbol

Changes to the definition apply to every instance at once.

1. Double-click the symbol's thumbnail in the Symbols section — or select an instance and click
   **Edit Symbol** in the Properties panel.
2. You are in **Symbol View**: the canvas shows only this symbol; edit shapes, colours and
   animation as usual.
3. Click the **`…`** row at the top of the Object Tree to return to the document.

{/* e2e-screenshot-plan  — BLOCKED (investigated 2026-09-01), needs an app change first
app: svga · viewport: 1440x900 · hideDevUi: true
show: Symbol View — the Object Tree with the "…" row at the top and the symbol's content on the canvas
setup: SOLVED up to the last step —
  setup(400, 400, {"fill": "#3d7de0", "stroke": "#1b3a63"}); click("toolsPicker.star"); move("canvas.bg", "source:120", "source:120", "canvas.bg", "drop:180", "drop:180"); click("toolsPicker.select");
  click("toolbar.actionsGroup-addSymbol"); click("toolbar.actionsGroup-addSymbol.makePrecomp")   // Symbol + first Clone instance
  click("toolbar.actionsGroup-addSymbol.svgClone"); move("canvas.el(nm:Clone_2,ty:use)", 150, 60) // second instance
  click("elementTree.blocks.header.title")                                                        // expands the SYMBOLS section
crop: clip covering the Object Tree plus the canvas
annotate: title chips: "The … row goes back up" (on the tree row) and "Editing the symbol content" (on the canvas)
notes: BLOCKER — Symbol View cannot be entered by script. Both documented routes end at a control with NO test-id:
  (1) the symbol THUMBNAIL in the SYMBOLS section (`elementTree.blocks`) — a double-click at an offset does land
      (the thumbnail highlights and the panel switches to the symbol's own properties: Content W/H, Aspect Ratio)
      but it does NOT enter Symbol View;
  (2) the **Edit Symbol** button in the instance's Properties panel (REFERENCE → Href row, `attrs.svg.href`) —
      offset clicks by px and by % were both tried and neither fires it.
  Double-clicking the instance ON CANVAS does nothing either. Unblocking needs a `data-test-id` on the Edit Symbol
  button (or on the symbol thumbnail) in the app — ask the owner before adding one. Do NOT retry with offset clicks.
*/}
<div class="shot small">Screenshot: Symbol View — the Object Tree with the "…" row at the top and the symbol's content on the canvas</div>

## Detach an Instance

To turn one instance back into ordinary, independent objects, select it and choose
**Object › Replace Link with its Referenced Content** (also a toolbar button). The instance
becomes a plain copy; the symbol definition and the other instances are unchanged.

## Delete Unused Symbols

A symbol without instances is labelled **Unused** in the Symbols section (and a red triangle
marks the collapsed section). Click the **bin** icon next to it to delete it. Symbols that still
have instances cannot be deleted.

## Symbols and the Saved File

Symbols export as `<symbol>` + `<use>` in SVG and JSON, and as pre-compositions in Lottie. In
Pixodesk Animator Studio's Lottie editor the same concept is called an **Animation Block**.
