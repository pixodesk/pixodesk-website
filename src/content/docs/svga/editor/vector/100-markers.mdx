---
title: "Markers"
description: "Markers on path starts, mids and ends — arrowheads, dots and decorations: create, apply, edit and delete them."
slug: "docs/svga/editor/markers"
sidebar:
  order: 100
draft: false
---

A **marker** is a small graphic that a path draws at its nodes — an arrowhead at the end, dots at
every node, a decoration at the start. Markers are defined once and referenced by any number of
paths, so a diagram with fifty arrows stores one arrowhead.

{/* e2e-screenshot
app: svga · viewport: 1440x900 · hideDevUi: true · date: 2026-09-01
setup: setup(400, 400, {"stroke": "#1b3a63"}); addBezier("exact", "M 50 300 C 90 190, 130 190, 170 250 C 200 295, 230 140, 270 130 C 305 122, 345 80, 260 100"); click("attrs.color.fill.content.type.noColor"); click("attrs.color.collapse"); input("attrs.stroke.width.input", "4"); click("attrs.transform.collapse"); click("attrs.endMarker.markerPicker"); click("attrs.endMarker.markerPicker.libMarker6"); click("attrs.midMarker.markerPicker"); click("attrs.midMarker.markerPicker.libMarker4")
crop: clip={"x":400,"y":100,"w":1035,"h":570}
annotate: annotate([{"kind":"title","theme":"red","halo":"chip","bounds":{"x":735,"y":252,"w":48,"h":28},"text":"End marker","at":"left","offset":{"x":-90,"y":-30},"arrow":true,"width":2.5,"arrowFill":"accent"},{"kind":"title","theme":"red","halo":"chip","bounds":{"x":634,"y":448,"w":28,"h":28},"text":"Mid markers","at":"bottom","offset":{"x":0,"y":30},"arrow":true,"width":2.5,"arrowFill":"accent"},{"kind":"title","theme":"red","halo":"chip","bounds":{"x":1041,"y":467,"w":383,"h":72},"text":"Marker slots","at":"left","offset":{"x":-60,"y":0},"arrow":true,"width":2.5,"arrowFill":"accent"}],{"scale":1.45})
notes: scale 1.45 (1035-px crop). THREE things had to be true or the picture shows nothing: (1) the path needs SEVERAL vertices — a 2-point path has no interior nodes, so mid markers never render (first attempt showed none); (2) fill must be NO COLOR — `attrs.color.fill.content.type.noColor`, since a filled path hides its own stroke and markers (setup's {"fill":"none"} does NOT do this, tinycolor cannot parse "none"); (3) stroke width 4 so the markers are big enough to read. The marker library popup's items have NO test-ids until it is OPEN — click the `markerPicker`, then `list_ids` shows `…markerPicker.libMarker0-8` (6 = arrowhead, 4 = dot). The path deliberately ENDS heading back left (last control pulls to 260,100) so the arrowhead does not sit under the bounding-box corner handle — it did in the first take. Collapse Colors and Transform to lift the marker rows above the fold. Chips use hand bounds — canvas marker instances have no test-id of their own.
*/}
![A curved path with dot markers at its interior nodes and an arrowhead at the end, beside the Start / Mid / End marker slots in the Properties panel](/images/docs/editor/vector/markers.png)

Three positions, each with its own marker (or the same one for all):

| Position | Where it appears |
| :-- | :-- |
| **Start Marker** | at the first node |
| **Mid Marker** | at every node in between — add nodes and you get more |
| **End Marker** | at the last node |

Markers follow the path direction; if an arrow points the wrong way, use
**Path › Reverse Path**.

## Create a Marker

1. Draw the marker shape (an arrowhead, a dot, …) and select it.
2. Choose **Object › Make Marker**, or click **Make Marker** in the toolbar.

The shape leaves the canvas and appears in the **Markers** section of the Objects panel, labelled
**Unused** until a path uses it.

## Apply a Marker to a Path

1. Select a path. (Markers only work on paths — convert shapes first with
   **Path › Object to Path**.)
2. In the Properties panel, open the **Start / Mid / End Marker** dropdowns and pick a marker —
   one of yours, or **Add from Library** for the built-in set.

If the dropdowns are missing, switch on the **Marker** section in the
[sections bar](/docs/svga/editor/edit-objects#properties-panel).

## Edit a Marker

Double-click the marker's thumbnail in the Markers section to enter **Marker View**, edit it like
any artwork, then click the **`…`** row at the top of the Object Tree to return. Every path using
the marker updates.

To make a variation, select the marker in the Markers section and copy / paste it — the copy
appears as a new, unused marker.

## Delete Unused Markers

Click the **bin** icon next to an **Unused** marker in the Markers section. A marker still used by
a path cannot be deleted until it is removed from that path (set the dropdown back to none).

## Markers and the Saved File

Markers are native SVG (`<marker>` + `marker-start / mid / end`) and are kept in SVG and JSON
output. Lottie has no markers — see [SVG ↔ Lottie support](/docs/svga#svg--lottie-support).
