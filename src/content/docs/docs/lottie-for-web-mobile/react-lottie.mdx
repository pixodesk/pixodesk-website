---
title: "React App"
description: "Lottie animations for React"
slug: "docs/add-lottie-to-react"
sidebar:
  order: 10
---
import SpacerBottom from '../../../../layouts/SpacerBottom.astro';
import { Badge } from '@astrojs/starlight/components';


# Add Lottie Animation to React App
<SpacerBottom />
You can add Lottie animations to a React app very easily. The most common and recommended way is to use the lottie-react package.

This is clean, modern way step by step.
<SpacerBottom />
---

## Create React project

```bash
cd path/to/your/projects
```
Then:

```bash
npm create vite@latest my-animation-app -- --template react-ts
```

This creates a React + TypeScript project called my-animation-app.

Now go into it:
```bash
cd my-animation-app
```
Install dependencies:

```bash
npm install
```

Start dev server:
```bash
npm run dev
```

You should see:
```bash
Local: http://localhost:5173
```

---

## Install Lottie for React
<SpacerBottom />
In your React project folder, run:

```bash
npm install lottie-react
```

---

## Make or take existing Lottie animation file
<SpacerBottom size='1rem' />
### Create an animation in 2D Animator
- <a href="https://pixodesk.com/2d-animator" target="blank">https://pixodesk.com/2d-animator</a><Badge text="Tool" variant="note" />

### Download from LottieFiles
- <a href="https://lottiefiles.com" target="blank">https://lottiefiles.com</a>


Create an assets folder (if does not exist) and add your animation:

```bash
src/assets/animation.json
```

---

## Use Lottie in your React Component
<SpacerBottom size='1rem' />
**src/App.tsx**
```tsx
import Lottie from "lottie-react";
import animationData from "./assets/animation.json";


function App() {
    return (
        <div>
            <Lottie 
                animationData={animationData}
                loop={true}
                autoplay={true}
                style={{ width: 300, height: 300 }}
            />
            {/* ... The rest of your code */}
        </div>
    );
}

export default App;

```

---


## Done! 🎉
<SpacerBottom />
Your Lottie animation should now work in your React project.
